module collections.symbole_table;

/*public struct SymbolTable(Key, Value) if(is_comparable!Key) {

	Value[Key] array;

	void put(Key key, Value val) {
		Value* p = key in array;
		if (p) *p = value;
		insert(key, value);
	}

	void insert(Key key, Value value) {
		Nullable!Key k;
		foreach(key_; array) {
			if (key_ > k)
	}
	Nullable!Value get(Key key);
	void erase(Key key);
	bool opBinaryRight(string op : "in")(Key key);
	bool isEmpty() const @property;
	size_t length() const @property;
	Key min() const @property;
	Key max() const @property;
	Key floor(Key key);
	Key ceiling(Key key);
	Nullable!size_t rank(Key key);
	Nullable!Key select(int k);



}
*/